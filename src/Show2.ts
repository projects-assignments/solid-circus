import { Show } from "./Show";

export class Show2 extends Show implements IShow2 {
    this.x = string

constructor(hour: number;
price: number;
artist: string;) {
    super(hour, price, artist);
    this.x = x;
}

result(): string {
    return Show2
}
}