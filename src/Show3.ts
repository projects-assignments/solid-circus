import { Show } from "./Show";

export class Show3 extends Show implements IShow3 {
    this.x = string

constructor(Hour: number,
    price: number,
    artist: string,) {
    super(Hour, price, artist);
    this.x = x;
}

result(): string {
    return Show3
}
}